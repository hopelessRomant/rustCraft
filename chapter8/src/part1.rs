// Storing Lists of Values with Vectors
pub fn part1() {
    let mut v: Vec<i32> = Vec::new();
    let list = vec![1,2,3];

    v.push(5);
    v.push(4);

    let one = &list[0];
    println!("first element of list is {one}");
}