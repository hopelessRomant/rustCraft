#[allow(dead_code)]
enum Face {
    Heads,
    Tails,
}

#[allow(dead_code)]
enum Color {
    Red,
    Blue,
    Green,
    Black,
}

#[allow(dead_code)]
enum Toss {
    Coin(Face),
    Dice(Color),
}

fn main() {   
}
